<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gallery App</title>

    <link rel="stylesheet" href="/css/bootstrap.min.css">
</head>

<body>
    <div class="container mt-5">
        <h1>Gallery</h1>
        <div class="row" id="gallery">
            <% for (let i = 1; i < 12; i++) { %>
            <div class="col-lg-3 col-md-4 col-sm-6 mb-4">
                <div class="card">
                    <img src="https://via.placeholder.com/150" class="card-img-top" alt="Image <%= i %>">
                    <div class="card-body">
                        <h5 class="card-title">Image <%= i %></h5>
                    </div>
                </div>
            </div>
            <% } %>
            <div class="col-lg-3 col-md-4 col-sm-6 mb-4" id="addButtonCard">
                <div class="card">
                    <button class="btn btn-primary" id="addButton">Add More</button>
                </div>
            </div>
        </div>
    </div>

    <script src="/js/bootstrap.bundle.min.js"></script>
</body>

</html>